# Semantic Objects

Current semantic objects are lightweight but real:

- Entities are extracted from title/summary terms.
- Claims are represented by item titles where no richer extractor is present.
- Topics come from source category or `General`.
- Evidence records include processor, confidence, raw output, normalized output, and evidence spans.

Low-confidence results enter review.
